<!-- <li> -->
  <tr>
    <td>
      <%= image_tag(pod.country.get_img_url, size: "30") %>
      <%= pod.country.name %>: <%= pod.short_date %> <%= pod.host_org.city_state %> -
      <%= pod.grantee.abbr %>
    </td>
    <td width="30px"></td>
    <td>
      <%= form_for task, :url => update_task_url(task) do |f| %>
        <%= f.hidden_field :completed, value: true %>
        <%= f.submit 'Mark Complete', class: "btn btn-primary btn-sm inline" %>
      <% end %>
    </td>
  </tr>
<!-- </li> -->
